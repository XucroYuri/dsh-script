{
  "name": "@script-studio/infra-object-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Immutable content hash and object lifecycle boundary for Script Studio",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["lib", "package.json"],
  "scripts": {
    "precheck": "pnpm --filter @script-studio/domain build && pnpm --filter @script-studio/contracts build",
    "check": "tsc -p tsconfig.json",
    "pretest": "pnpm --filter @script-studio/domain build && pnpm --filter @script-studio/contracts build",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@script-studio/contracts": "workspace:*",
    "@script-studio/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
