{
  "name": "@script-studio/infra-postgres",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL authority schema and tenant-boundary migration for Script Studio",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["lib", "migrations", "package.json"],
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@script-studio/contracts": "workspace:*",
    "@script-studio/domain": "workspace:*",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
