# Stage 2 Verification Report

Date: 2026-09-03  
Status: Passed

## Scope

Stage 2 delivered the dual-host minimum vertical slice:

- shared `Host Contract v1` and `DevHostApi`;
- Codex marketplace plugin with Skills and MCP stdio server;
- DeepSeek Harness Bundle with Host service, tools and Client Slot;
- one deterministic local fixture shared by both adapters.

The fixture is explicitly local-development-only. PostgreSQL, object storage, production authentication, CRDT collaboration and offline outbox remain Stage 3/4 work.

## Verified Behavior

- capabilities negotiation;
- Team/IP/Project/Season/Episode hierarchy read;
- episodic Season + first Episode creation with expected revision;
- Team member authorization and stable `forbidden`/`revision-conflict`/`invalid-state` errors;
- request-hash idempotency replay and conflict isolation;
- equivalent Codex/DSH adapter results for the shared parity contract;
- Codex plugin manifest, Skills and MCP stdio tool execution;
- DSH Host HTTP route, registered tools and official Client Slot composition;
- plugin uninstall removes the host entry without deleting shared fixture state;
- DSH exact `.tgz` package installation and composition.

## Automated Results

```text
pnpm check: passed
pnpm test: passed (51 test files / 356 tests, including 301 historical Bundle tests)
pnpm build: passed
pnpm pack:audit: passed (historical Bundle)
pnpm pack:audit:stage2:dsh: passed (12 packed files)
pnpm test:stage2:codex: passed (Codex 0.150.1 marketplace + MCP)
pnpm test:stage2:dsh: passed (Harness 0.1.0-rc.7 composition)
pnpm test:stage2:package-install: passed (exact tarball)
git diff --check: passed
```

The DSH package audit reported 12 allowlisted files, 32,417 packed bytes and 134,148 unpacked bytes. The exact tarball composition installed `@script-studio/dsh-adapter@0.1.0` in an isolated Harness profile and removed it successfully.

## Deferred By Design

- PostgreSQL schema, RLS and transactional outbox;
- S3-compatible object storage and content lifecycle;
- OIDC/OAuth 2.1 production sessions;
- realtime Draft collaboration and event recovery;
- production cloud API and durable multi-user data.

## Exit Decision

All Stage 2 exit gates in `docs/spec/migration-plan.md` are satisfied. Stage 3 may begin with the cloud single-user authority slice. No user project data, production credentials or real model calls were used by this verification.
